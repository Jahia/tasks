query TaskBoard($first: Int!, $after: String, $search: String) {
    taskBoard(first: $first, after: $after, search: $search) {
        totalCount
        pageInfo {
            hasNextPage
            endCursor
        }
        edges {
            node {
                id
                title
                creator
                owner
                state
                possibleOutcomes
            }
        }
    }
}
