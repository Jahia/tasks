query WorkflowActivity($path: String!) {
    workflowActivity(path: $path) {
        activeTasks {
            label
            dueDate
        }
        history {
            label
            endTime
        }
    }
}
