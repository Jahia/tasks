query TaskBoard($first: Int!, $after: String) {
    taskBoard(first: $first, after: $after) {
        totalCount
        pageInfo {
            hasNextPage
            endCursor
        }
        edges {
            node {
                id
                title
                creator
                owner
                state
                possibleOutcomes
            }
        }
    }
}
