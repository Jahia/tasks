export const TEST_SITE_KEY = 'tasks-e2e-test';

// This module ships no template set of its own (jahia-module-type=system, embeddable on
// any site) -- point this at whatever base/starter template set the target Jahia instance
// provisions. Adjust when wiring this suite into CI/provisioning for a specific instance.
export const TEST_TEMPLATE_SET = 'dx-base-demo-templates';

export const PAGE_SIZE = 20;
